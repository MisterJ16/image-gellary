import React from 'react';

export const GalleryContext: any = React.createContext({});

export const HeaderContext: any = React.createContext({});

export const FooterContext: any = React.createContext({});
